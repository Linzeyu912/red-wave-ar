<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CODE-08：二维码扫描需要相机。AR Optional：不要求 ARCore（ADR-0001）。 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 相机和自动对焦均为可选硬件；无相机设备仍可通过手动列表进入虚拟展馆。 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RedWaveAR"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:exported="true"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustResize">

            <!-- 适配：横竖屏均可（fullSensor），跟随重力传感器。
                 计划书 §2.3 原“固定横屏”调整为“可旋转，VR 推荐横屏但不强制”。
                 Activity 自行处理 configChanges，旋转不重建，UI 与传感器补偿在 Compose 层响应。 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
