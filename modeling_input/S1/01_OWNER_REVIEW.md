# S1 用户补充素材审核清单

> 状态：`OWNER_REVIEW_REQUIRED`
> 对象：`USER-VIS-001` 当前入口、`USER-VIS-002` 当前浮雕墙
> 说明：内部风格研究已经放行；以下问题只决定图片发布、识图和下一阶段建模边界。

> 提交入口：完成本文件后，按 `00_START_HERE.md` 第 5 节把任务交给建模模型。

## REV-01 摄影与来源

请分别确认两张照片是谁拍摄的：

- [ ] `USER-VIS-001` 是我本人/项目团队拍摄；
- [ ] `USER-VIS-001` 来自第三方，来源是：__________；
- [ ] `USER-VIS-002` 是我本人/项目团队拍摄；
- [ ] `USER-VIS-002` 来自第三方，来源是：__________。

当前文件没有可用 EXIF，不能仅靠图片证明摄影者、拍摄时间和地点。

## REV-02 允许用途

请为两张图分别确认：

| 用途 | 推荐默认值 | USER-VIS-001 | USER-VIS-002 |
|---|---|---|---|
| 项目内部研究 | 允许 | 允许 / 不允许 | 允许 / 不允许 |
| 抽象提取建筑与材质特征 | 允许，不复制独特作品 | 允许 / 不允许 | 允许 / 不允许 |
| App 信息卡显示原图 | 暂不允许 | 允许 / 不允许 / 待确认 | 允许 / 不允许 / 待确认 |
| 加入识图数据库 | 暂不允许 | 允许 / 不允许 / 待确认 | 允许 / 不允许 / 待确认 |
| 打印为触发卡 | 暂不允许 | 允许 / 不允许 / 待确认 | 允许 / 不允许 / 待确认 |
| 答辩/宣传使用 | 暂不允许 | 允许 / 不允许 / 待确认 | 允许 / 不允许 / 待确认 |
| 完整临摹或重建 | 不允许 | 允许 / 不允许 / 待确认 | 允许 / 不允许 / 待确认 |

即使摄影者允许，第二张还包含当前纪念馆的完整浮雕作品；在馆方或作品权利状态未明确前，推荐保持“可观察材质、不可复制作品”。

## REV-03 地点与时间

- [ ] 两张都拍摄于平西情报联络站纪念馆；
- [ ] 第一张拍摄日期大约为：__________；
- [ ] 第二张拍摄日期大约为：__________；
- [ ] 第二张就是现馆题为“胜似雄兵十万”的浮雕墙；如名称不准确，请改为：__________。

## REV-04 是否把浮雕墙作为现场识图对象

推荐选择：**MVP 不使用浮雕墙作为识图对象，继续采用原创触发卡 + 二维码兜底。**

原因：第二张技术特征丰富，但若目标是现场实物，仍需验证不同距离、斜角、日照、遮挡和设备的识别率，也需要确认场馆允许该用途。

- [ ] 同意推荐方案，本阶段不测试浮雕墙识图；
- [ ] 希望把浮雕墙列为候选，后续安排现场测试；
- [ ] 浮雕只做内容/材质参考，永不作为识图对象。

## REV-05 建模交接闸门

推荐选择：**建模模型先审核现有白盒，再交三张简单材质风格图，用户审核通过后才进入正式环境和文物中模。**

- [ ] 同意 `STYLE-01` 游客起点、`STYLE-02` 操作区 45°、`STYLE-03` 路线区回望三图闸门；
- [ ] 需要修改固定视角或增加审核图：__________。

## 快速回复模板

如果接受所有推荐项，可以直接回复：

```text
同意推荐方案。
USER-VIS-001 摄影者/来源：
USER-VIS-002 摄影者/来源：
两张是否均拍摄于平西情报联络站纪念馆：
大致拍摄日期（不知道可写未知）：
其他允许用途：无；先只做内部研究与抽象建模参考。
```

收到回复后，资料审核模型更新本目录的 `material_manifest.csv` 和权利状态；如果仍按推荐方案，不需要等待图片发布许可即可让建模模型开始三张风格图。
