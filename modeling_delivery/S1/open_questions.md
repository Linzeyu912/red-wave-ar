# S1 建模当前待确认项

> 更新日期：2026-07-20
> 唯一输入入口：`modeling_input/S1/00_START_HERE.md`
> 原则：只保留仍然有效的问题；已关闭的早期阻塞不再重复列出。

## 1. 已关闭

- `CLOSED-01`：S1 确认为有史料依据的原创虚拟主题展馆，不做现馆或战时原址 1:1。
- `CLOSED-02`：1943 年冬涧沟村秘密电台为物理核心，1947 年清风店战役为结果信息层。
- `CLOSED-03`：`p_s1_codebook` 技术 ID 保留，用户名称为“电文与保密通信资料包”，只做无铭文通用复原。
- `CLOSED-04`：当前纪念馆入口只提取灰砖/石基/旧木等高层语汇，不制作完整入口。
- `CLOSED-05`：`modeling_input/S1/` 已交付，白盒 v0.2 已生成并通过静态 GLB/scene 检查。
- `CLOSED-06`：风格方向、红色上限、主材质、灯光层级和 STYLE-01/02/03 固定视角已冻结。

## 2. 不阻塞三张风格图，但阻塞正式资产

| ID | 问题 | 当前处理 | 解锁条件 |
|---|---|---|---|
| OQ-R-01 | 电台具体型号、尺寸、面板和铭牌 | 只用无品牌通用箱式设备 | 馆方展签、四视图、尺寸和原件/复制品身份 |
| OQ-K-01 | 发报键具体制式、尺寸和材质 | 只用通用直键 | 多角度实物照片、尺寸和身份说明 |
| OQ-E-01 | 战时山洞/院落精确结构 | 使用原创山村掩护 + 山洞行动层 | 测绘、现场空间资料或馆方确认 |
| OQ-E-02 | 暖光灯具具体形态 | 风格图只用无品牌抽象任务光 | 同期灯具来源或项目负责人确认原创灯具方向 |
| OQ-M-01 | 精确交通路线 | 只用原创抽象节点和线路 | 馆方/地方志路线核对 |
| OQ-P-01 | 人物、姓名与肖像 | 不建人物，只用无名光点/声音 | 姓名、肖像、照片和内容审核完成 |
| OQ-U-01 | 两张用户图片的发布/识图权 | 已关闭：仅内部研究与抽象建模；App、识图、触发卡、宣传和完整复刻均禁止 | 若用途改变，重新完成来源与权利审核 |

## 3. 白盒与编码联调待确认

| ID | 问题 | 当前值 | 责任方 |
|---|---|---|---|
| OQ-I-01 | `rotation_deg` 与 -Z 朝前的真机 yaw 基准 | 白盒暂用 `[0,0,0]`，起点看向电台 | 编码 + 建模真机联调 |
| OQ-I-02 | 拾取由 Filament `View.pick` 还是应用碰撞实现 | 节点名 + scene.json 交互半径双兼容 | 编码负责人 |
| OQ-I-03 | 主展示机、帧率、碰撞和 20 次进出验证 | 尚未完成真机记录 | 编码/测试负责人 |

## 4. 用户当前只需审核

用户已于 2026-07-20 完成 `modeling_input/S1/01_OWNER_REVIEW.md`；批准内部研究与抽象建模用途，并同意三张风格图闸门。随后用户否决旧版 8 m × 8 m 白盒的空间尺度和粗略 radio；当前必须执行 `modeling_input/S1/04_COMPACT_CELLAR_REVISION.md` 的 M3D-01R，再审核新的三图。旧版 W1–W8/D-01–D-03 只保留为问题来源对照，不得据此进入正式资产。

## 5. 下一次更新

建模模型交付 STYLE-01/02/03 和 `style_review_report.md` 后，在本文件记录：

- 三图是否通过；
- 白盒是否需要最小修复；
- 哪些 OQ 被新素材关闭；
- 是否解锁 M3D-02/M3D-03，以及具体解锁范围。

### 2026-07-20 交付记录（建模模型）

- 三张风格图与 `style_review_report.md` 已交付（`preview/style/` 六图，1600×900 + 1280×720）。
- 白盒静态审核 25 PASS / 0 FAIL，**无需最小修复**；GLB 与 scene.json 本轮未改动。
- 白盒是否需要修复的最终结论待真机联调；三图是否通过待用户审核。
- 新增/激活的决策项（详见报告 §1.2 W1–W6、§4 D-01–D-03）：
  1. 桌区位置：现行 `[0.80,1.50]`（视线 1.70 m，满足计划书 §4.3「2 m 首目标」）vs `scene_layout_brief` PROPOSED `[1.20,-1.00]`（视线 3.5–4.5 m）——两处口径存在张力，需项目负责人定夺；
  2. 门位置（南墙 x≈-1.05）导致 STYLE-01 起点视角无入口意象（D-01），建议与上项合并决策；
  3. 信息面宽度 4.40 m vs 推荐 2.8–3.6 m；信息面薄 collider 是否补充；
  4. 预览候选"导览蓝地点卡"是否补入白盒（D-02）；
  5. `visitor_start.rotation_deg` 轴向基准（OQ-I-01/OQ-I-04）仍待真机联调确认。
- M3D-02/M3D-03 **未解锁**：等用户"通过/指定修改项" + 资料审核复核 + 上述决策项关闭。

### 2026-07-20 交付记录（建模模型，M3D-01R 修订轮）

- **旧轮次已被取代**：上方 M3D-01 审核轮记录仅留档对照。旧版 8 m × 8 m 白盒与其 W1–W8、D-01–D-03 决策项随旧布局一并作废——W3–W8、D-01、D-02 的对象（信息面宽度/桌区位置/门位置/地点卡）在紧凑小室布局中已不存在，不再要求在旧布局上修补；W1/W2（yaw 基准、锚点口径）以新白盒重新带入真机联调。
- 新紧凑储洞小室（净 5.3 × 4.7 × 2.25 m）与审核级 `p_s1_radio`（2644 tri）已交付；`whitebox_selfcheck.py` **34 PASS / 2 WARN / 0 FAIL**，Khronos 4/4 0 错 0 警，check_glb 4/4 PASS。
- 新三张风格图与六版本（1600×900 + 1280×720）已交付并逐图目检；红色 0.00%/0.00%/0.04%，暖光桌焦点成立，暗部 p02 0.113–0.125。
- 本轮修正：电台 `rotation_deg [0,180,0]` 使控制面朝游客；线缆终点截短避免悬空；渲染器支持道具 yaw。
- 新增/激活的待确认项：
  1. OQ-I-04：`rotation_deg` 真机 yaw 基准（radio 首次实际使用 yaw 180°，真机联调时统一确认）；
  2. mp_radio 距 radio 1.01 m 为近看检查点，首看建议带 1.4–1.9 m 以 visitor_start（1.64 m）为锚——记录项，非缺陷；
  3. 凳子为无朝向方凳白盒，形态与朝向留待正式阶段（不改已验收比例）。
- M3D-02/M3D-03 **未解锁**：等用户与资料审核对新三图给出"通过/指定修改项"，并完成编码真机联调。

### 2026-07-20 审核通过与冻结记录（M3D-01R-P，提交 d0341c1）

- 用户**通过**本轮"紧凑地下储洞小室 + 审核级电台 + STYLE 审核图呈现修订"：尺度符合小型储洞工作室方向；六图全画幅、无烧字；电台控制面朝向正确、手机构图可读；`integration_checklist.md` 内容完整。
- **冻结**：M3D-01R-P 的几何、scene.json、GLB、相机构图与风格图即刻冻结，不再做呈现层微调；任何坐标/起点/交互锚点改动须与编码模型真机联调确认，scene.json 保持唯一事实源。
- 仍然有效：OQ-I-04/WR-1（yaw 基准，待真机联调定案）、OQ-I-03（真机记录）、OQ-R-01/K-01/E-01/E-02/M-01/P-01（阻塞正式资产）。
- **下一步闸门**：编码模型按 `integration_checklist.md` 完成真机联调（起点朝向、陀螺仪 yaw、碰撞、移动边界、拾取、高亮锚点）并回传；通过前 M3D-02 核心文物正式建模不解锁，M3D-03/高模/PBR/人物/地图保持禁止。
